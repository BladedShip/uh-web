import { HeroSection, AboutSection } from "@/components/blocks";

const Home = () => {
    return (
        <>
            <HeroSection />
            <AboutSection />
        </>
    );
};

export default Home;
